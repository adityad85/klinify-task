const saveImage = (imageFile) => {
  // lorem pixel link was not working properly so changed it with this link
  return Promise.resolve('https://picsum.photos/800/200');
};

export default saveImage;
